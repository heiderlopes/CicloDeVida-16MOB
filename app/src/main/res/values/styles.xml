<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="botao_personalizado">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">16dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:shadowColor">#000000</item>
        <item name="android:shadowDx">1</item>
        <item name="android:shadowDy">1</item>
        <item name="android:shadowRadius">2</item>
    </style>

    <style name="botao_tres_pontos" parent="botao_personalizado">
        <item name="android:background">@drawable/green_button</item>
        <item name="android:drawableLeft">@drawable/basquete_3pontos</item>
    </style>
    <style name="botao_dois_pontos" parent="botao_personalizado">
        <item name="android:background">@drawable/yellow_button</item>
        <item name="android:drawableLeft">@drawable/basquete_2pontos</item>
    </style>
    <style name="botao_um_pontos" parent="botao_personalizado">
        <item name="android:background">@drawable/blue_button</item>
        <item name="android:drawableLeft">@drawable/basquete_1ponto</item>
    </style>
    <style name="botao_reiniciar" parent="botao_personalizado">
        <item name="android:background">@drawable/red_button</item>
        <item name="android:drawableLeft">@drawable/reload</item>
    </style>
</resources>
